(ns undirected.tree
  (:require [undirected.connected :refer [connected-components connected?]]
            [undirected.graph :as g]
            [utils :refer [???]]))

(defn tree? [graph]
  (???))

(defn forest? [graph]
  (???))
